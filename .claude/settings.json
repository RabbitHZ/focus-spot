{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; [[ \"$f\" == */api/*.py ]] && /Users/hwa.j/.local/bin/uvx ruff format \"$f\" 2>/dev/null || true; }",
            "timeout": 30,
            "statusMessage": "ruff formatting..."
          }
        ]
      }
    ]
  }
}
